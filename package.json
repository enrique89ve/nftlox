{
	"name": "nftlox",
	"private": true,
	"engines": {
		"node": ">=18.0.0"
	},
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev:indexer": "bun run --filter nftlox-indexer dev",
		"dev:playground": "bun run --filter nftlox-playground dev",
		"test": "bun run --filter '*' test",
		"test:sdk": "bun test packages/sdk",
		"test:indexer": "bun test packages/indexer",
		"typecheck": "bun run --filter '*' typecheck",
		"sync:auth": "bun run scripts/sync-auth-levels.ts",
		"sync:auth:check": "bun run scripts/sync-auth-levels.ts --check"
	}
}
